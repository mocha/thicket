// The frontend is a pure client of the API. No server-side rendering yet; public
// collection pages (stage 3) are where SSR earns its keep.
export const ssr = false;
export const prerender = false;
